# grantucorsa — 開発ルール

公開情報と物理モデルだけで**トヨタ86（ZN6）のデジタルツイン**を構築するプロジェクト。
目的は「見た目がリアル」ではなく**物理的整合性**。

基準車両: **ZN6 前期 GT 6MT 日本仕様（DBA-ZN6-D2E8 / FR / FA20）**

---

## 判断を要するルール（絶対）

### 1. 実車データを捏造しない

数値が見つからないときの正解は**空欄のまま残すこと**。埋めることではない。
`vehicle.json` では `"unknown"` と書く。

### 2. 出典のない数値を実測値として扱わない

「一般的に○○とされる」「よく知られている」は出典ではない。
どの資料の何ページか、どの URL かを `source` に残せない数値は `assumed` 以下に置く。

### 3. ラップタイムだけを合わせる最適化を禁止する

シミュレーション結果が実測値と合わないとき、**まず入力データを疑う。**
パラメータをいじって数値を合わせにいくのは最後の手段であり、Level 2〜3 に限られる。

よくある事故:
- トルクカーブを2点補間 → 中回転域が速すぎる → タイヤμを下げて辻褄合わせ
- ファイナルを取り違え → 駆動力が10%不足 → μを上げて辻褄合わせ

**どちらも入力データの誤りであって、パラメータの不確実性ではない。**

### 4. 物理計算と表示用3Dモデルを完全に分離する

物理は `vehicle.json` の寸法から独立に構築する。
3Dモデルの頂点座標・コリジョンメッシュを物理計算に流用しない。

### 5. 既存コードを読まずに変更しない

特に `Physics/` と `Vehicles/**/*.json`。
「たぶんこうなっている」で書き換えない。

### 6. エラーを隠蔽しない

`try/except: pass` を書かない。テストを skip して通さない。
物理的にあり得ない値が出たら、それを握りつぶさずに報告する。
**失敗した状態を「完成」と呼ばない。**

---

## パラメータの変更権限

| Level | 権限 | 対象 `source` |
|---|---|---|
| **0** | **変更禁止** | `official` |
| 1 | 範囲内のみ・理由必須 | `official_marketing` / `measured` / `secondary` |
| 2 | 探索可能 | `estimated` |
| 3 | 完全探索 | `assumed` |

**Level 0**: エンジン排気量、ホイールベース、公式ギア比、公式車重。
**ファイナルギア比は variant の選択も保護対象**（GT は 4.100、G 6MT のみ 3.727）。

---

## この車固有の罠（3つ）

1. **ファイナルは単一値ではない。** G 6MT のみ 3.727、GT / GT"Limited" / 6AT 全車は 4.100。
   取り違えると約10%の駆動力誤差が入る。基準車両は **GT なので 4.100**。

2. **全高 1,320mm はアンテナ込み。** ルーフ高は **1.285m**。
   二次情報に多い 1,300mm はどちらとも一致しない。3Dスケール補正には `roof_height` を使う。

3. **FA20 は 4,000rpm 付近にトルクの谷がある。**
   最大出力点と最大トルク点の2点だけで補間するとこの特性が消える。
   トルクカーブは複数データポイント必須。

---

## FR であることの帰結

CL1（FF）前提で書かれた元仕様書から変わった点。

- **加速時の荷重移動が駆動輪（後輪）に乗る。** FF とは逆。定荷重モデルは過小評価側に外れる
- **パワーオーバーステアを起こす。** AIドライバーに後輪スリップ率監視とスピン検出が要る。
  PID の目標値追従だけでは、コーナー脱出のたびにスピンして1周できない
- **後輪の複合スリップ（Combined Slip）が限界を決める。** 実装を「後期」に回せない
- 駆動系にプロペラシャフトが入る（`Gearbox → Prop Shaft → Final/Diff → Drive Shaft → 後輪`）
- GT はトルセンLSD標準。Open Diff は比較基準としてのみ残す

---

## スコープ

**今回は Phase 1〜8。** 第1完成目標は「灰色の ZN6 が物理的に妥当な動きでテストコースを1周する」。

- **UE5 は Phase 8 まで導入しない。** 可視化は matplotlib で足りる
- **Blender は Phase 11 まで導入しない**
- 3D・音響・実在コース（Phase 9〜15）には**一切触らない**。`Docs/SPEC_PHASE2_BACKLOG.md` へ退避済み

`Unreal/` `Blender/` `Audio/` を**先に空で作らない。**

---

## ドキュメント

必要なときだけ読む（自動ロードしない）。

| ファイル | 内容 |
|---|---|
| `Docs/SPEC_ZN6.md` | **仕様書 兼 要件定義書。現行版** |
| `Docs/ZN6_BASELINE.md` | 基準車両の確定理由、罠2件の詳細 |
| `Docs/SOURCE_A_VERIFICATION.md` | 公式諸元表との照合記録。**このPDFはテキスト抽出できない** |
| `Docs/HANDOFF.md` | 方針変更の経緯（履歴） |
| `Docs/DATA_COLLECTION_ZN6.md` | 収集項目リスト（14カテゴリ） |
| `Docs/SPEC_PHASE2_BACKLOG.md` | 退避したフェーズ |
| `Docs/RULE_CLASSIFICATION.md` | 憲法18ルールの三層分類とその理由 |
| `Docs/SPEC_CL1_original.md` | **編集禁止。** 差分の基準点 |

**矛盾したら `SPEC_ZN6.md` > `HANDOFF.md` > `SPEC_CL1_original.md`。**

パス限定のルールは `.claude/rules/` にある（`Physics/` `Vehicles/` を触るとき自動で読まれる）。
コミット前の機械的チェックは `.claude/hooks/` にある。

---

## 作業場所の分担

Claude Code とチャット（claude.ai）は**コンテキストを共有しない**。

- Claude Code: 実装・テスト・コミット
- チャット: 公開情報の調査と出典整理、「この数値は物理的に妥当か」の監査

**同一コンテキストで自己検算しない。** 「作るAI」と「疑うAI」を分けるための構成。
